# Insights Data Model

Responses conform to `schemas/AiResponse.schema.json`.

Key fields:
- `narration`: string
- `actions`: array of action DTOs
- `cards`: array of UI cards
- `metadata`: model/version/tokens

