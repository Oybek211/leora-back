APP_NAME=leora-backend
APP_ENV=local
APP_PORT=9090
APP_BASE_PATH=/api/v1
JWT_SECRET=changeme
JWT_ACCESS_TTL=1h
JWT_REFRESH_TTL=24h

# Render sets PORT automatically; locally defaults to 9090
PORT=9090

# Database: set DATABASE_URL for Render, or use individual fields for local dev
DATABASE_URL=
DB_HOST=postgres
DB_PORT=5432
DB_USER=leora
DB_PASSWORD=leora
DB_NAME=leora_dev

# Redis: set REDIS_URL for Render, or use individual fields for local dev
REDIS_URL=
REDIS_ADDR=redis:6379
REDIS_PASSWORD=
REDIS_DB=0

# CORS: comma-separated allowed origins (empty = localhost defaults)
CORS_ORIGINS=

# OAuth / Apple Sign-In
GOOGLE_OAUTH_CLIENT_ID=
APPLE_BUNDLE_ID=com.sarvar.leora
APPLE_TEAM_ID=QP47XLF76J
APPLE_KEY_ID=
